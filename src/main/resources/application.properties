# Server Configuration - CRITICAL for Render
server.address=0.0.0.0
server.port=${PORT:8080}

# PostgreSQL Database Configuration for Render
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Actuator Health Endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.org.springframework.web=INFO

# ==========================================
# EMAIL CONFIGURATION
# ==========================================

# For Gmail (free, up to 500 emails/day)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=airgilstudio@gmail.com
spring.mail.password=lexo jbxp vwrf aqcp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# For SendGrid (recommended for Render, 100 emails/day free)
# spring.sendgrid.api-key=your-sendgrid-api-key

# Email sender details
app.email.from=noreply@salonbooking.com
app.email.support=support@salonbooking.com